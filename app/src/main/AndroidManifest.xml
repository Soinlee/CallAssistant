<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Telephony-related permissions (e.g. READ_PHONE_STATE) imply the telephony
         hardware feature; declare it as optional so the app is still installable on
         non-telephony (tablet / ChromeOS) devices. -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <application
        android:name=".application.Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning,UnusedAttribute">
        <activity
            android:name=".activity.MainActivity"
            android:clearTaskOnLaunch="true"
            android:configChanges="orientation|screenSize"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.SettingsActivity"
            android:exported="true"
            android:label="@string/action_settings"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="com.github.soinlee.callassistant.action.PLUGIN_SETTING" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.LicensesActivity"
            android:exported="true"
            android:label="@string/license"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="com.github.soinlee.callassistant.action.VIEW_LICENSES" />
                <action android:name="com.github.soinlee.callassistant.action.VIEW_PRIVACY" />
                <action android:name="com.github.soinlee.callassistant.action.VIEW_FEATURE" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MarkActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:noHistory="true"
            android:taskAffinity=".activity.MarkActivity"
            android:theme="@style/Theme.AppCompat.Light.Dialog" />
        <!--
            Foreground service hosting the caller-info floating window.
            type="specialUse" is required for targetSdk 34+; every StandOutWindow
            that shows a persistent window promotes itself via startForeground().
        -->
        <service
            android:name=".service.FloatWindow"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Display a persistent on-screen floating window with caller identification info during phone calls" />
        </service>
        <service
            android:name=".service.MarkWindow"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Display a floating window for marking a phone number" />
        </service>

        <!--
            Static receiver kept only for the BOOT_COMPLETED / QUICKBOOT_POWERON
            broadcasts, used to re-start the persistent foreground service after a
            reboot. The phone-state (incoming / outgoing call) broadcasts are now
            registered dynamically in Application (static registration of
            NEW_OUTGOING_CALL is unsupported on Android 8+ and PROCESS_OUTGOING_CALLS
            The old android:permission="RECEIVE_BOOT_COMPLETED" attribute was
            incorrect: BOOT_COMPLETED has no permission restriction and that attribute
            would only block the broadcast from being delivered.
        -->
        <receiver
            android:name=".receiver.IncomingCall"
            android:exported="true">
            <intent-filter android:priority="999">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="org.xdty.phone.number.API_KEY"
            android:value="${API_KEY}" />
        <meta-data
            android:name="org.xdty.phone.number.JUHE_API_KEY"
            android:value="${JUHE_API_KEY}" />
        <meta-data
            android:name="org.xdty.phone.number.LEANCLOUD_APP_ID"
            android:value="${LEANCLOUD_APP_ID}" />
        <meta-data
            android:name="org.xdty.phone.number.LEANCLOUD_APP_KEY"
            android:value="${LEANCLOUD_APP_KEY}" />
    </application>

</manifest>